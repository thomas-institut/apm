Attach the following PHPDoc template to every API route definition in `src/www/index.php`:

```php
/** 
  * TODO: fill up description
  *
  * API Inventory:
  *    Method: {method}
  *    Authentication: user token
  *    Uses action: TBD
  *    PHP Unit Test: no
  *    PHP Input Schema: TBD
  *    PHP Output Schema: TBD
  *    ApiClient Method: TBD
  */
```

Change `{method}` to the route's method (GET, POST, AND). 
Do not attempt to generate any other information.