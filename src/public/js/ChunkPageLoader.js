// Loads the ChunkPage class into a global variable so that it can be accessed
// from an inline script in a twig-based dynamic page

import {ChunkPage} from './ChunkPage'
window.ChunkPage = ChunkPage
